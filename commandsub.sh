#!/bin/bash


files=$(ls -al)
#this is called commaned substitution, it means putting the output of a command inside a variable 


#to use special characters wiithout consiidering it as a speciial character in the command we use / backslash before the char
echo "Files in directory: ->->->->-> $files"
echo "\$100 "


